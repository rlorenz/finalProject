
<%= form_for(@a_class) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    
    <%= f.text_area :name, placeholder: "The name of the class..." %>
  </div>
  <%= f.submit "Add class", class: "btn btn-large btn-primary" %>
<% end %>
